# Live News React App

A simple React application that fetches live news articles using the NewsAPI and displays them in a responsive Bootstrap card layout.

## Features
- Fetches live news articles (currently set to Bitcoin).
- Responsive grid layout using Bootstrap.
- Loading spinner and error handling.
- Clean modular components 
